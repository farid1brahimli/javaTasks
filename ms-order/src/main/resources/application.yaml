spring:
  application:
    name: ms-order
  datasource:
    url: jdbc:postgresql://localhost:5433/order_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
server:
  port: 8081